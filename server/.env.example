# ═══════════════════════════════════════════════════════════
# Task Manager K2 — Konfiguracja serwera
# ═══════════════════════════════════════════════════════════
# Skopiuj ten plik jako .env i uzupełnij wartości:
#   cp .env.example .env
# ═══════════════════════════════════════════════════════════

# ── Azure AD (Entra ID) — rejestracja aplikacji ──
AZURE_TENANT_ID=twoj-tenant-id
AZURE_CLIENT_ID=twoj-client-id
AZURE_CLIENT_SECRET=twoj-client-secret

# ── SharePoint — identyfikatory witryny i list ──
SHAREPOINT_SITE_ID=twoja-domena.sharepoint.com,id-witryny-1,id-witryny-2
SHAREPOINT_TASKS_LIST_ID=id-listy-zadania
SHAREPOINT_TEAM_LIST_ID=id-listy-zespol

# ── Powiadomienia e-mail ──
NOTIFICATION_SENDER_EMAIL=nadawca@twoja-domena.pl
PM_EMAIL=pm@twoja-domena.pl
MANAGER_EMAIL=kierownik@twoja-domena.pl

# ── Tryb danych ──
# sharepoint = dane z SharePoint Online (wymaga powyzszej konfiguracji)
# local      = dane lokalne z pliku data.json (do testow offline)
DATA_MODE=sharepoint

# ── Autoryzacja frontendowa ──
# false = brak logowania w przegladarce (tryb deweloperski)
# true  = logowanie MSAL przez konto Microsoft (produkcja)
FRONTEND_AUTH=false
